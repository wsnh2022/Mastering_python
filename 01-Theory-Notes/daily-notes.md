# learned concepts

## day 1:
- CLI important commands like `cd`, `cd **[TAB]`, `ls/dir`, `mkdir`, `cwd`, `code script.py`, `python script.py`
- pip packages using `python -m pip --version`, `pip list`, `python -m pip install <package-name>`
-
