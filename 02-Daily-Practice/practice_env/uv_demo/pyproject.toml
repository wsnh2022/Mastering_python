[project]
name = "uv-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "wsnh2022", email = "wsnh.code5621@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "rich>=15.0.0",
]

[project.scripts]
uv-demo = "uv_demo:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
